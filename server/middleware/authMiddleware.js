const protect = async (req, res, next) => {};

// TODO: Middleware to be implemented post basic function completion.
// Validating `accessToken` in firebase's UserImpl to confirm authenticated access only.

module.exports = { protect };
